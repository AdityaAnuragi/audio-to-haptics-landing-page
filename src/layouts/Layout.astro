<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
<title>audio-to-haptics</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	*, *::before, *::after { box-sizing: border-box; }
	html { scroll-behavior: smooth; }
	html, body {
		margin: 0;
		padding: 0;
		width: 100%;
		font-family: system-ui, -apple-system, sans-serif;
		color: #111;
	}
	/* H) Mesh gradient */
	body {
		background-color: #fafafa;
		background-image:
			radial-gradient(ellipse 50% 55% at 10% 18%,  rgba(251, 146, 60,  0.35) 0%, transparent 100%),
			radial-gradient(ellipse 50% 55% at 90% 25%,  rgba(59,  130, 246, 0.35) 0%, transparent 100%),
			radial-gradient(ellipse 45% 50% at 88% 80%,  rgba(251, 146, 60,  0.28) 0%, transparent 100%),
			radial-gradient(ellipse 45% 50% at 12% 82%,  rgba(59,  130, 246, 0.28) 0%, transparent 100%),
			radial-gradient(ellipse 55% 50% at 50% 50%,  rgba(139, 92,  246, 0.30) 0%, transparent 100%);
	}
	a { color: inherit; }
	pre, code { font-family: 'Cascadia Code', 'Fira Code', 'Courier New', monospace; }
</style>
