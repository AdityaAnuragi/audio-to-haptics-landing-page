<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
<title>audio-to-haptics</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	*, *::before, *::after { box-sizing: border-box; }
	html { scroll-behavior: smooth; }
	html, body {
		margin: 0;
		padding: 0;
		width: 100%;
		font-family: system-ui, -apple-system, sans-serif;
		color: #111;
	}
	body {
		background-color: #fff;
		background-image:
			radial-gradient(ellipse 80% 60% at 0% 50%, rgba(124, 58, 237, 0.45) 0px, transparent 100%),
			radial-gradient(ellipse 70% 55% at 100% 20%, rgba(192, 100, 255, 0.4) 0px, transparent 100%),
			radial-gradient(ellipse 65% 50% at 50% 100%, rgba(100, 50, 220, 0.35) 0px, transparent 100%);
	}
	a { color: inherit; }
	pre, code { font-family: 'Cascadia Code', 'Fira Code', 'Courier New', monospace; }
</style>
